import type { ActorRef, Subscribable } from 'xstate';
export declare function isActorWithState<T extends ActorRef<any>>(actorRef: T): actorRef is T & {
    state: any;
};
export declare function useSelector<TActor extends ActorRef<any, any>, T, TEmitted = TActor extends Subscribable<infer Emitted> ? Emitted : never>(actor: TActor, selector: (emitted: TEmitted) => T, compare?: (a: T, b: T) => boolean, getSnapshot?: (a: TActor) => TEmitted): import("vue").ShallowRef<T>;
