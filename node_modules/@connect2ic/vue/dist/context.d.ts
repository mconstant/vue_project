import type { InjectionKey, Ref } from "vue";
import type { createClient } from "@connect2ic/core";
export declare type ContextState = {
    client: ReturnType<typeof createClient>;
    dialog: {
        open: () => void;
        close: () => void;
        isOpen: Ref<boolean>;
    };
};
declare const contextKey: InjectionKey<ContextState>;
export { contextKey };
